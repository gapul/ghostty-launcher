# kitty 設定スニペット
# ~/.config/kitty/kitty.conf に追加

# グローバルホットキーで起動するためのスクリプトを用意:
#   ~/.config/launcher/terminals/kitty-toggle.sh

# kitty.conf:
# map cmd+space launch --type=overlay bash ~/.config/launcher/terminals/kitty-toggle.sh
