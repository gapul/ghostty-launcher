# ダウンロード生成物（install.fish で取得）
icon_map.sh

# ランタイムキャッシュ（/tmp に生成されるが念のため）
*.cache

.DS_Store

# Rust build artifacts
launcher-search/target/

# Built binary (regenerated by install.sh)
core/launcher-search

# User data (generated at runtime)
frecency.txt
